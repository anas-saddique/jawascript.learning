const myObject ={
     js : 'jawascript',
     cpp : 'c++',
     rb : 'ruby'
}  
for (const key in myObject) {
console.log(`${key} shoutcut is for ${myObject[key]} `)
}

const programing = ["js","html","css"]
for (const key in programing) {
  console.log(programing[key])   
}
 